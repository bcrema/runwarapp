interface:
  display_name: "Cloud SQL Postgres MCP"
  short_description: "Acesso read-only ao Cloud SQL PostgreSQL via MCP."
  default_prompt: "Use $gcloud-db-access para consultar Cloud SQL PostgreSQL via MCP."
dependencies:
  tools: []
